# 02 · Taste 宪章（什么优化值得做）

## 第一性原理：推理团队只关心一本账

> **满足 SLO 前提下的 $/M tokens**（每百万 token 成本）

每个实验结尾必须能写："**这省了 X% 的资源 Y，在条件 Z 下**"。资源 Y 只有五种合法取值：

1. **GPU 时间**（避免重算、减少空转）
2. **HBM 容量**（显存让给更大 batch / 更长上下文）
3. **生命周期损耗**（重启、预热、模型切换）
4. **跨节点带宽税**（PD 分离的代价项）
5. **SLO 余量**（尾延迟稳 → 敢少预留机器）

**凡是不落在这五项里的优化，都是自嗨。**

## 宪章七条

1. 每个实验回答一个经济问题：省了哪种资源、省多少、在什么条件下
2. 报 p99 不报 max；报 workload 形状不报裸数字；所有结论注明适用 regime
3. 用真实 trace 不用均匀随机：agentic 多轮、共享前缀、scan 流量都要有
4. 每个优化给出离硬件极限的距离（roofline 式表述），不满足于"变快了"
5. 被拒绝的方案和数据写进设计文档，与采用的方案同等篇幅
6. 规模声明诚实：所有结论标注实验规模，不外推到未测的尺度
7. 优先做"能改变决策"的测量，不做"只是更好看"的测量

## 场景地图（七条 JD 线的拓扑）

量化/投机解码 = 正交技术（可不做）。
前缀缓存、长上下文、PD 分离、KV 存储、agentic 优化——**五条河汇入同一个湖：KV cache 的复用与生命周期**：

- 前缀缓存 = 复用机制
- 长上下文 = 容量压力来源
- PD 分离 = 跨节点传输税
- agentic = 复用率爆表又制造 scan 流量的 workload 形状
- KV 存储 = 湖本身

锚定"KV cache 的复用与生命周期"，五条线自然路过。

## 规模焦虑的化解

- **规律是尺度不变的，只有常数随尺度变**：延迟阶梯、命中率-容量曲线、fetch-vs-recompute 交叉点、scan 下 LRU 崩溃——1 卡与 1000 卡同构。
- **小的比较优势是归因干净**：生产舰队是噪声地狱；单机的完美方差控制 = 因果归因 > 大而脏的相关性。
- **KV 研究的一半不需要 GPU**：策略层（准入/淘汰/预取/前缀共享/上限估算）用公开 trace 在笔记本上做。GPU 只在数据通路实测与端到端验证时需要（短租、脉冲式）。

## Taste 与问题的分工

- Taste = 品味过滤器，**提前训练**（读 DeepSeek 报告 / PegaFlow 博客 / Mooncake 论文 = 训练集）
- 问题感 = 异常嗅觉，**只能生长**（从测量里长）
- 宪章负责过滤，测量负责产生；宪章每两个月被实践修订一次
