# 04 · 生长方法论（项目怎么长出来）

## 核心循环：异常捕猎

```
① 有模型   —— 先能心算/roofline 出"应该是多少"（没模型的异常只是噪声）
② 被冒犯   —— 实测 ≠ 预测（异常 = 候选切口）
③ 挖到金属 —— 归因到硬件层常量
④ 翻译到账 —— 落成五项资源之一
```

**"对不上的数字 = 异常 = 候选切口。卡住不是因为找不到目标，是因为还没有数据在冒犯你。baseline 的真正意义是给了数据冒犯你的机会。"**

注意：是"反**你的模型**"不是"反常识"。冒犯度是理解深度的仪表——模型越准，能冒犯它的数据越少，但每个越锋利。
若"哪儿都不对"，说明模型没建好——先回 micro-bench 把延迟阶梯亲手量一遍。

## 三台目标发生器

### 发生器一：knob 扫描（baseline 跑起来第一周）
```
上下文长度:  1K → 4K → 16K → 32K
前缀共享率:  0% → 50% → 95%
请求速率:    空闲 → 打满
缓存预算:    抠门 → 宽裕
重启/切换:   kill 引擎，看什么死掉、重建多久
```
每个点问："这个数字符合我的心智/roofline 模型吗？"

### 发生器二：假设清单（逐条用测量审问默认假设）
```
"block size 就该是 16 tokens"  → 测不同 block size 下 NVMe 效率曲线
"淘汰就该用 LRU"               → agentic trace 重放，看 scan 污染
"命中率是最重要指标"           → HLL 上限，看距天花板多远
"远程命中不如本地重算"         → 测 fetch-vs-recompute 交叉点
"冷数据就该压缩"               → MLA latent 量化 vs 直存性价比
```

### 发生器三：issue tracker 挖矿（借别人的生产环境）
vLLM / Mooncake / LMCache / PegaFlow 的 issue 列表 = 公开的别人之痛目录。
**开放 >3 个月、多人 +1、维护者没空的 issue = 为你准备的切口。**

## 评分函数（异常收日记本，选总分最高的一个）

> **痛感**（归到五项资源哪项、省多少）× **归因深度**（能否追到硬件层）× **可触达性**（两台机器能否修）

然后写 goals/non-goals，开工。

## PegaFlow 考古案例（生长史事实骨架）

| 时间 | 事件 |
|---|---|
| 2025-11-07 | xiaguan 个人仓库创建，首日全量落库；定位"**单机多卡** vLLM v1 KV 卸载"；hello world 用 GPT-2 |
| 个人阶段 | 发现 vLLM 空闲块分配乱序破坏 DMA 连续性 → 一行排序补丁 + **上游 RFC #31371** |
| 2025-11→2026-01 | 个人孤军两个月（25 star）；TTFT 572.5→61.5ms 实验此时完成 |
| 2026-01-05 | 迁入 novitalabs 组织仓库（PR 编号从 #23 起） |
| 2026-01 前四天 | PR #2 批量准入+CUDA stream 异步拷贝+O_DIRECT；#10 SSD 批量+背压+指标；#23 分配粒度；#24 RPC 指标 |
| 2026-05-18 | vLLM 官方联合博客（500GiB 池启动 2.15×、8 实例共享 +56%、MLA TP8 去重 +72%、RDMA 194GB/s） |
| 2026-09 | #450 前后：混合 mamba 哈希错位 root-cause 文学（"用户问 A 模型答 B"），顺路挖出 Mooncake/vLLM 同款缺陷 |

## 生长算法（从 PegaFlow 提炼）

```
0. 痛点必须是"账"，不是"词"     —— 用数字表达的异常
1. 洞察收敛成一句话             —— 整个架构是它的推论
   （"KV cache 是长生命周期的服务资产，不是进程的临时状态"）
2. 先写 non-goals 再写代码      —— 疆域小到必胜
3. 骨架先行，profiling 牵路     —— 每个 PR = 一层瓶颈 + 一个看护指标
4. 深水区即护城河               —— 沉默的正确性 bug 是分水岭
5. 跟着模型 release 走          —— 生态替你产生新角度
```

## 三条修正

1. **楔子可以再小一个数量级**：他的起点是"单机、一个引擎、一个方向"。你的第一天也可以是"租一张卡，让 vLLM 的 KV 落到内存再回来，测明白每一微秒"。
2. **第一天就写 non-goals**：它是过滤器不是计划书——"路线图会过期，过滤器不会"。
3. **社区闭环不用等"够格"**：他的第一个上游动作 = 一行补丁 + 一个 RFC。你的第一个可以是"复现了 X 的 benchmark，我的环境数字是 Y，这是脚本"。**可复现性即诚意。**

## 阶段地图（滚动规划，非军令状）

- **P0（第 1~2 周）**：baseline 现场——装 vLLM + 一个 KV connector（LMCache 或 PegaFlow），knob 扫描，异常日记本开张；亲手测延迟阶梯 micro-bench
- **P1（第 3~6 周）**：选定第一个切口（评分函数最高分）→ 闭环陈述 v1 → 第一篇公开笔记
- **P2（第 7~12 周）**：切口闭环 → 上游贡献（issue/PR）→ 决策记录（含被拒绝方案）
- **P3（第 13~16 周）**：汇总技术报告；若某切口自然长大成独立系统，接住（副产品，不追求）

每一阶段结束重审宪章与本文档。
