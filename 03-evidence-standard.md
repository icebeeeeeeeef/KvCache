# 03 · 证据标准（TL 的 180 秒扫描怎么过）

## TL 真正在找的三样东西

1. **抽象之下的深度**：故事里最底层的名词是什么？是"配置项"，还是 doorbell / SQ depth / PCIe TLP / 延迟阶梯上的具体数字
2. **可被复核的真实**：baseline 公平性、复现脚本、threats to validity
3. **同频**：研究的是他们的模型结构（MLA）、他们的栈（3FS/vLLM）、他们的话语（诚实的局限分析）

## 证据成色分级

| 档次 | 表面动作 | TL 内心 OS |
|---|---|---|
| L1 教程级 | docker 拉起 vLLM、调参、"TTFT 降了 30%" | 培训班三天能教出来 |
| L2 工程师级 | nsys/eBPF 定位到传输只吃满 PCIe 40%，改完给前后 trace | 能干活，约一面 |
| L3 DeepSeek 级 | 归因到瓶颈**类别**并造机制消灭它，给 roofline 分析 | 这人已经在做我们的工作 |

L1 与 L3 表面动作相同，差别只在归因下沉到哪一层。

## 完整闭环陈述 = 三句话

**① 结果句**：
> 在 [硬件] + [技术栈/版本] 上，面对 [workload 形状]，满足 [SLO] 的前提下，相比 [baseline]，[资源 Y] 改善了 X%。
> （baseline 与资源 Y 五项之一，不可省略——没有 baseline 的数字是轶事）

**② 归因句**：
> 因为 [机制]：哪一层、什么常量、什么结构导致旧 baseline 的损耗。
> （有这句，数字才从"碰巧变快"变成"知识"）

**③ 边界句**：
> 该结论在 [条件] 外不成立/未验证。
> （诚实性印章；主动画边界比吹"全面超越"可信一万倍）

**尾巴**：每条陈述挂可复现入口（脚本 + 原始数据 commit hash）。
> 闭环的终极定义：**一个陌生人能在不看你的情况下，按你的文档重现你的数字。**

## 范式示例（PegaFlow 真实数字套模板）

> ① H800 单机 TP8 + vLLM，DeepSeek-V3.2 MLA 长前缀复用负载，500GiB 固定预算，相比 in-process 缓存吞吐 +72%（资源 2：有效缓存容量）。
> ② 因为 MLA latent KV 与 TP rank 无关，in-process 物理存 8 份；进程边界把存储粒度从"每 rank"变"每逻辑块"。
> ③ 仅适用 MLA 类结构；MHA 无此收益；未验证跨节点。

## 终态证据组合（六件套）

1. 旗舰 repo（README 首屏一张 TL 十秒看懂的图）
2. 技术报告（中英双语：方法论、消融、局限）
3. micro-bench 数字墙（亲手测的 HBM→PCIe→RDMA→NVMe→GDS 延迟阶梯）
4. 3~5 个 merged PR（3FS 优先，vLLM/LMCache/Mooncake 其次）
5. 一份公开性能审计报告（对 3FS/Mooncake 的善意对抗性审计）
6. 公开失败日志（假设-证伪记录）

## 反模式清单（一票否决）

- JD 复读机：buzzword 全列、没有一个数字
- 无方法论的 benchmark：无 baseline、无复现路径的"超过 Mooncake 40%"
- NIH 无由造轮子：说不清"现有方案在哪个测量点不成立"
- 只读书不发货 / 只粘胶水不下沉
- 零失败的完美叙事（不如一页《我测错的五个假设》）

## 负面结果的定价

"我测了 GDS 在我的 block size 下相比 io_uring+零拷贝只快 3%，但引入 cuFile 部署复杂度，所以拒绝"——
这一句话 > 简历上"熟悉 GDS/SPDK/RDMA"一整行。
"Alternatives Considered" 是资深工程师设计文档的家族特征。
